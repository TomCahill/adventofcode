{
  "name": "adventofcode-2023",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "npm run build && tsc -w",
    "build": "rm -rf ./build/ && tsc && npm run copyTxt",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/index.js",
    "copyTxt": "copyfiles -u 1 src/**/*.txt build/"
  },
  "author": "Tom Cahill",
  "license": "",
  "dependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.2"
  }
}
